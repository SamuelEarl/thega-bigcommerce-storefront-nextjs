export enum Variant {
  Primary = "primary",
  Secondary = "secondary",
  Success = "success",
  Danger = "danger",
  Warning = "warning",
  Info = "info",
}

export enum ElementWidths {
  Auto = "auto",
  Full = "full",
  FitContent = "fit-content",
}

export enum Colors {
  Primary = "primary",
  Secondary = "secondary",
  Success = "success",
  Danger = "danger",
  Warning = "warning",
  Info = "info",
  Neutral = "neutral",
}

export enum Sizes {
  XSmall = "xs",
  Small = "sm",
  Medium = "md",
  Large = "lg",
  XLarge = "xl",
}

export interface SizeStyles {
  all: string;
}
